server:
  port: 8888

spring:
  application:
    name: config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: file://${user.home}/git/greetings/config
        git:
          uri: file://${user.home}/git/greetings
          basedir: config